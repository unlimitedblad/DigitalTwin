# Flask RAG 聊天机器人配置文件

# API配置
DASHSCOPE_API_KEY=your_dashscope_api_key_here
CHAT_MODEL=qwen-plus
EMBED_MODEL=text-embedding-v3

# 日志配置
# 日志级别: DEBUG（详细）/ INFO（默认）/ WARNING / ERROR
LOG_LEVEL=INFO
# 日志文件路径（支持相对路径）
LOG_FILE=logs/app.log
# 单个日志文件最大字节数（默认 10 MB）
LOG_MAX_BYTES=10485760
# 日志文件保留数量（轮转备份数）
LOG_BACKUP_COUNT=5

# CSV 数据源配置（支持 glob 通配符，如 *.csv 导入全部，或指定单个文件名）
CSV_FILE_PATTERN=zhenhuan_chat.csv

# ChromaDB 本地向量数据库配置（无需安装数据库服务器）
# 向量数据存储在本地文件夹中
CHROMA_PERSIST_DIR=./chroma_db
CHROMA_COLLECTION=wechat_embeddings

# RAG配置
RAG_ENABLED=true
RAG_MAX_RESULTS=20
RAG_MAX_CONTEXT_LENGTH=2000
RAG_INCLUDE_METADATA=true

# Flask服务配置
FLASK_HOST=0.0.0.0
FLASK_PORT=8080
FLASK_DEBUG=false

# 模型参数配置
TEMPERATURE=0.5
TOP_P=0.7
MAX_TOKENS=500
REPETITION_PENALTY=1.2
